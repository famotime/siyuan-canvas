# 无界 —— 想象”无界”，思考有方

## v1.4.0
### 新增
- **演示模式**：
  - 支持按连线关系逐卡片播放演示，支持录制和播放自定义演示路径；
  - 支持多种播放效果，包括焦点卡片显示比例、背景透明度等；
- **视频卡片**：嵌入B站和Youtube视频的卡片，支持直接播放；
- **移动端适配**：支持移动端仅查看模式并实现双指捏合缩放；支持移动端打开 Canvas 查看器；
- **动态浮动面板**：右侧面板支持“钉住”与“动态浮动”显示模式；

### 改进
- 修复设置不显示标题栏后，卡片无法拖拽移动的问题；
- 修复嵌入图片节点预览问题和跟随卡片大小自适应问题；
- 其他UI和交互细节优化；

## v1.3.5
### 新增
- 支持颜色主题切换，内置 4 套彩虹色系预设；
- 重布局（beta）：选中节点后沿连线关系自动重排连接节点，保证节点位置不重叠，降低手工排布工作量；
- 支持在思源文档中插入 Canvas 预览块，点击即可跳转到无界编辑器；

### 改进
- 连线面板和右侧栏的节点名称支持点击跳转到对应节点；
- 文档树右键菜单新增复制文件路径命令；
- 更换工具栏图标；
- 修复关闭调试日志开关后仍有日志输出的问题；

## v1.3.0
### 新增
- 输入 `[[` 或 `【【` 快速添加笔记卡片，支持上下键快速选择笔记或块；
- 双击画布空白区域快速创建文本卡片；
- 支持文本卡片与思源文档/块/图片卡片互相转换；
- 选中思源文档或块关联卡片后，可按空格键弹出浮窗预览；
- 文档树新增右键上下文菜单，支持重命名、复制、删除、打开所在目录、新建子文件夹和新建文档；
- 选区面板新增连线信息，可查看节点各端点的连接关系；

### 改进
- 修复缩略图视口范围坐标转换、拖拽块预览刷新、标题块递归刷新等同步与显示问题；
- 修复拖动单个卡片时其他卡片视觉位置跟随移动的问题；
- 重复添加卡片时会自动偏移位置，避免新卡片与已有卡片重叠；
- 其他UI细节改进；

## v1.2.0

### 新增
- 支持画布导出为PNG图片；
- 支持将思源文档或文本节点一键拆解为章节卡片；
- 支持快速新建卡片：Tab 新建子卡片，Enter键新建兄弟卡片；
- 新增画布缩略图显示开关；
- 思源关联节点浮动工具栏新增刷新同步功能；
- 支持通过“搜easy”插件搜索/替换卡片内容（思源文档节点仅支持搜索，不支持替换）；
- 支持批量修改卡片属性；

### 改进
- 思源文档卡片双击后会在前台打开关联文档并跳转到关联位置；
- 关联标题块的卡片会显示标题下内容；
- 拖拽改变卡片大小后会自适应显示图片、文本等内容；
- 修复链接卡片无法编辑URL问题；
- 图标、卡片显示样式等UI细节优化；

## v1.1.0
### 新增
- 支持便捷文档处理：新建文件、子文件夹、重命名、排序；
- 支持从思源笔记文档树拖入文档方式创建卡片；
- 支持网页链接卡片；
- 支持编辑撤销和重做操作；
- 支持画布内卡片关键词搜索和定位；
- 支持画布缩略图；
### 改进
- 日常操作和快捷键帮助；
- 大量UI细节改进；

## v1.0.6
### 新增
- 设置页新增调试日志开关（默认关闭）
- 设置页工作区路径不可编辑
### 改进
- 修复 SiYuan v3.6.1 putFile API 文件名校验失败问题
- 其他UI细节改进

## v1.0.5
### 新增
- 新增删除 Canvas 文件功能（工作区文档列表和最近文件列表）
- 添加文件节点时，搜索命中关键词高亮显示
### 改进
- 打开新文件前检查未保存更改，避免数据丢失
- 未命名画布自动编号，防止重名覆盖
- 嵌入文件卡片内滚动时阻止画布缩放
- 搜索结果按文档→块→图片排序，增加分组标题
- 连线标签分层渲染，添加文字描边避免被线条遮挡
- 其他细节优化

## v1.0.2

根据集市上架评审意见修复相关细节问题。

## v1.0.1

根据集市上架评审意见优化细节。

## v1.0.0

无界 siyuan-canvas 首版本。

在思源笔记中打开、编辑和导出 Obsidian `.canvas` 画布文件。基于开放的 JSON Canvas 格式，实现两个笔记工具之间的画布互通。

- 底层遵循 `obsidianmd/jsoncanvas` 开放规范
- 保存时会保留规范未定义的额外字段，确保与 Obsidian 兼容互通
- 当前版本优先保证文件格式兼容和核心编辑体验，不追求与 Obsidian Canvas 的完全视觉一致

### 卡片类型

| 卡片 | 用途                             | 举例                                   |
| ------ | ---------------------------------- | ---------------------------------------- |
| **文本**     | 写下想法、笔记、备注             | 记录会议要点、写下灵感碎片             |
| **文件**     | 关联思源中的文档、图片或其他画布 | 把相关笔记卡片拖到画布上，形成知识地图 |
| **分组**     | 将相关卡片归类分组               | 按主题、阶段或优先级分组               |

- 文本卡片支持 Markdown 渲染，标题、列表、代码块、引用等都能直接预览
- 文件卡片能识别思源文档、块 ID、图片素材和嵌套画布，双击即可跳转到对应内容

### 连线与关系表达

- 从卡片四边的锚点拖拽即可创建连线
- 连线支持添加文字标签，标注关系类型（如"导致"、"属于"、"参考"）
- 可设置箭头方向（无箭头、单向、双向）和颜色
- 选中连线后可拖拽端点重新连接到其他卡片，或直接拖到空白处删除
